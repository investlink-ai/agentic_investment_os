# Threat model

This document owns material threat enumeration, current control coverage, and residual risk for the
[accepted topology](architecture.md#system-topology). It does not own architecture, investment rules,
control behavior, or test procedure; each mitigation and verification entry links to that fact's
canonical owner.

## Scope and assumptions

- V0 is local, single-operator, US-equity research with Alpaca paper authority. Live capital and
  multi-user operation are outside scope.
- The local operator and operating-system account are trusted roots. Host or operator compromise can
  bypass application controls; this design provides detection and refusal, not host-level tamper
  resistance.
- External text, source and broker data, model output, durable representations, and copied Lab inputs
  are untrusted at every receiving seam.
- Production research is implemented through `UpdateMemory`, deterministic Balanced and required
  same-input shadow construction through `ConstructPortfolio`, and atomic Champion decision and
  optional Balanced packet publication through `PublishDecision`. Execution, evaluation, and later
  lifecycle phases remain [scaffolded](../README.md#status). A required target control is not
  implementation evidence.
- Loss of availability is preferable to unsupported research, corrupted authority, duplicated
  exposure, credential disclosure, or an unreconstructable decision.

## Protected assets

| Asset | Required property |
| --- | --- |
| Investment and governance authority | The operator alone approves policy; deterministic `portfolio` alone sizes positions and constructs packets; credentialed `execution` alone validates packet intent and causes broker effects |
| Broker credentials and account context | Credentials stay inside executor composition and out of models, research, logs, fixtures, and durable artifacts; non-secret account scope is packet-bound and independently revalidated |
| Evidence and model-visible decision inputs | Provenance-bound, cutoff-correct, content-identified, bounded, and reconstructable |
| Financial history, packets, and receipts | Append-only or immutable, integrity-checked, idempotent, and reconstructable from authoritative state |
| Process capabilities and state roots | Least-authority composition, disjoint Lab and production state, explicit private paths, and no ambient effect acquisition |

## Threat register

| Threat | Protected asset and entry seam | Required controls and owner | Verification owner | Residual risk |
| --- | --- | --- | --- | --- |
| Poisoned evidence or embedded prompt instructions | Research integrity at source adapter → Evidence Vault → model-input seams | Treat external text as inert data; bind content, source, mapping, availability, and cutoff provenance; admit only bounded evidence and declared tools. [Defensive patterns](defensive-patterns.md#treat-external-text-as-data) and [investment rules](investment-domain.md#research-workflow) own the controls. | [Recorded evidence contract](../tests/contract/test_recorded_evidence.py), [official evidence contract](../tests/contract/test_recorded_official_evidence.py), and [evidence/research scenarios](testing.md#evidence-memory-and-research) | A well-formed source can still be false, coordinated, or misleading. Schema and provenance validation do not prove truth; role challenge, abstention, operator review, and prospective evaluation retain that risk. |
| Model output, prompt, or tool authority escalation | Research, governance, portfolio, and execution authority at model port → research-validator seams | Treat model output as hostile; accept only closed role schemas and local evidence references; reject weights, orders, lifecycle, governance, credential, memory-write, and undeclared-tool directives. Production composition pins the exact role policy and exposes only an explicit research-owned model port with inert tool schemas. Deterministic modules retain downstream authority under [authority and trust](architecture.md#authority-and-trust). | [Model contract](../tests/contract/test_research_lab_model.py), [production research lifecycle](../tests/integration/test_production_research_lifecycle.py), [Lab resolution journey](../tests/integration/system/test_research_lab_resolution_journey.py), and [research scenarios](testing.md#evidence-memory-and-research) | Validators enforce encoded contracts, not semantic honesty. A well-formed model artifact can still make a poor inference; Skeptic challenge, CIO abstention, operator review, and prospective evaluation retain that risk. |
| Stale, incomplete, contradictory, or substituted portfolio material | Investment authority at recorded input, persisted production resolution, policy, and portfolio-construction seams | Pin the complete portfolio policy and input identities into the run; validate canonical positions, cash, units, prices, consecutive code-pinned exchange sessions, histories, groups, the complete event set and its actual-release rather than schedule subtype, research provenance, source, availability, and Data Regime once at entry; reconstruct the exact terminal production resolution set from durable calls without a new model effect; persist one independently hash-validatable HouseView, Balanced result, and complete same-input shadow set; and bind lifecycle history to the result set's exact run and recorded instant. Missing, future, stale, altered, Lab, cross-run, or partial material returns a typed refusal or invalid-history disposition. [Portfolio rules](investment-domain.md#portfolio-construction), [lifecycle](architecture.md#session-lifecycle), and [configuration](config-catalog.md#portfolio-cycle-policy) own the controls. | [Recorded portfolio contract](../tests/contract/test_recorded_portfolio.py), [portfolio unit and property tests](../tests/unit/test_portfolio.py), and [portfolio lifecycle and corruption tests](../tests/integration/test_production_research_lifecycle.py) | Complete typed inputs can still estimate risk poorly or encode a misleading sector, liquidity, common-cause, correlation, or event classification. Fixed caps, cash preservation, operator governance, and prospective evaluation retain that model risk. |
| Calendar substitution, delayed launch, duplicate scheduler invocation, or scheduler capability bleed | Lifecycle timing and idempotency at clock/calendar/launch-agent → scheduler process → public lifecycle process seams | Pin the complete supported NYSE calendar and versioned policy; normalize an injected aware clock to UTC; refuse new eligible effects in unsupported years while preserving status, bounded missed classification without lifecycle effects, and recovery of already-started supported sessions; reconstruct due and missed sessions from append-only history; append intent before `Advance`; serialize live processes with an OS-released private lock; and call only public `Advance` and `Status` through a process client under a stable policy-and-session identity. The scheduler process never imports or retains lifecycle composition, model, packet, signing, account, executor, broker, or credential capabilities. A heartbeat and `launchd` state never establish lifecycle liveness. [Scheduler architecture](architecture.md#configuration-and-deployment), [ADR 0010](adr/0010-isolate-market-session-scheduler-history.md), and [configuration](config-catalog.md#market-session-scheduler-policy) own the controls. | [Scheduler integration](../tests/integration/test_scheduler.py) and [scheduler process journey](../tests/integration/system/test_scheduler_journey.py) | A compromised host clock or operator can suppress launches or alter all local state. The code-pinned calendar expires after 2026 and deliberately stops new-session availability until a reviewed version replaces it. A hung process holds the live lock and delays another invocation; V0 prefers visible unavailability to overlapping lifecycle requests. |
| Credential disclosure or capability bleed | Broker credentials, signing material, and account scope at configuration → entrypoint → packet → executor-process seams | Secret references resolve only in their credentialed composition. The operating-system entrypoint receives typed in-memory signing and verification ports plus a non-secret content-identified paper scope, but no account lookup, real account identifier, broker credential, execution port, or network client. The executor receives no model capability, while the operating system and Lab receive no broker credential. [Architecture](architecture.md#system-topology), [module rules](module-graph.md#rules), and [configuration](config-catalog.md#decision-publication-composition) own the separation. | [Signing unit contract](../tests/unit/test_decision_signing.py), [packet hostile contract](../tests/contract/test_decision_packet_contract.py), [module/effect gates](testing.md#static-architecture-gates), [runtime configuration](../tests/integration/test_runtime_configuration.py), and [signed-packet system journey](../tests/integration/system/test_lifecycle_journey.py) | Packet signing and operating-system isolation are executable. The future executor process and its credential resolution remain scaffolded, so end-to-end broker-credential isolation is not yet executable acceptance evidence. Host or operator compromise remains outside application containment. |
| Governance, configuration, or packet tampering and replay | Constitution, pinned policy, account scope, and order authority at operator/configuration/packet-store seams | Sign and version governance, activate it only at an exact future session, pin complete decision material and hashes, and publish one Champion Decision Record and optional packet atomically. Exact-field hostile readers independently validate canonical identity, scope, cycle, expiry, policy, risk, content hash, signature, and semantic agreement with the complete durable Balanced-plus-shadow cycle. Packet admission uses the pinned exchange calendar, permits only the fifteen-minute pre-open freeze window, and expires authority thirty minutes after open. Fresh publication rejects off-window or expired material; exact retry reparses and replays the immutable winner, while changed decision or authorization material conflicts. [Architecture](architecture.md#safe-execution-handoff), [ADR 0011](adr/0011-atomically-publish-one-balanced-decision.md), and [product requirements](product-requirements.md#portfolio-and-execution) own the controls. | [Packet-window unit contract](../tests/unit/test_decision_window.py), [packet hostile contract](../tests/contract/test_decision_packet_contract.py), [packet construction and properties](../tests/unit/test_portfolio.py), [atomicity, concurrency, replay, expiry, and corruption](../tests/integration/test_decision_publication.py), [lifecycle recovery](../tests/integration/test_advance_lifecycle.py), and [signed-packet system journey](../tests/integration/system/test_lifecycle_journey.py) | Packet construction, timing admission, signing, account binding, atomic publication, replay, and status reconstruction are implemented. The future executor must independently revalidate the same packet against then-current account and market state; that second validation and all broker effects remain scaffolded. A privileged local actor can still alter both state and key material. |
| Duplicate or unresolved broker effects | Paper exposure and receipts at executor → broker request/observation seams | Validate the packet before intent; persist effect-local intent before submission; use stable client order identity; append independent broker facts; reconcile ambiguous timeout before retry. [Effect ordering](architecture.md#safe-execution-handoff) and [defensive patterns](defensive-patterns.md#persist-intent-before-effects) own the controls. | Required [portfolio/execution](testing.md#portfolio-and-execution) and [fault-injection](testing.md#external-contracts-and-fault-injection) scenarios | Execution is scaffolded. Broker observations may remain late, partial, contradictory, or unavailable; unresolved state must block discretionary progress rather than infer success. |
| Ledger rewrite, coordinated suffix truncation, or projection substitution | Evidence, belief, governance, lifecycle, production model-call, portfolio, decision, and outcome authority at persistence/reopen seams | Append corrections and validate each ledger's canonical hashes and references. For belief history, require its singleton terminal anchor to equal the complete chain; for production research, revalidate intent and observation material and re-derive allowed terminal role paths; for portfolio history, reparse Balanced and every required shadow and re-derive their semantics; for decision history, reparse the canonical Decision Record and packet, verify its signature, reconstruct it from the exact portfolio cycle, and bind the lifecycle checkpoint, run, cycle, identities, expiry, no-action reason, and recorded instant. For all state, require the exact current schema and rebuild projections without granting them authority. [Durable architecture](architecture.md#durable-state) and [defensive patterns](defensive-patterns.md#anchor-the-terminal-length-of-append-only-history) own the controls. | [Belief recording](../tests/integration/test_belief_recording.py), [lifecycle status](../tests/integration/test_lifecycle_status.py), [production and portfolio lifecycle](../tests/integration/test_production_research_lifecycle.py), [decision publication](../tests/integration/test_decision_publication.py), [SQLite schema](../tests/integration/test_sqlite_schema.py), and [durability scenarios](testing.md#lifecycle-and-durability) | Lifecycle references detect removal or substitution of completed production calls, Balanced results, required shadows, and decision publications. Other unreferenced ledger rows may not detect a coordinated rollback to a valid prefix; a privileged local actor can also alter or delete all state and corresponding signing material. V0 provides no external notarization, replicated ledger, backup, or disaster recovery guarantee. |
| Filesystem traversal, symlink escape, or Lab/production overlap | Credentials, repository contents, and authoritative state at configured-path → filesystem seams | Resolve explicit absolute roots, reject unsafe or symlinked paths, use private ignored storage, keep Lab state disjoint, and never use source directories as runtime state. [Filesystem patterns](defensive-patterns.md#use-private-explicit-filesystem-roots) and [configuration](config-catalog.md#runtime-configuration) own the controls. | [Runtime configuration](../tests/integration/test_runtime_configuration.py), [Evidence Vault](../tests/integration/test_evidence_vault.py), [lifecycle persistence](../tests/integration/test_advance_lifecycle.py), and [Research Lab replay](../tests/integration/test_research_lab_replay.py) | Filesystem validation cannot contain a compromised host account or privileged process. Backup and disaster recovery remain unimplemented; loss of portfolio history must block reconstruction rather than infer a prior decision. |
| Oversized, slow, unavailable, or quota-limited external input | Availability and bounded local resources at source/model/parser seams | Enforce schema and size bounds, attention caps, timeouts, durable refusals, resource metadata, and no metered fallback. Never relax authority or reuse stale material to preserve availability. [Fail-closed patterns](defensive-patterns.md#fail-closed-with-a-durable-reason) and [subscription constraints](product-requirements.md#operational-constraints) own the controls. | [Model contract](../tests/contract/test_research_lab_model.py), [production research lifecycle](../tests/integration/test_production_research_lifecycle.py), [evidence capture](../tests/integration/test_evidence_capture_lifecycle.py), and [fault injection](testing.md#external-contracts-and-fault-injection) | Local resource exhaustion and provider outage can stop a session. V0 accepts bounded denial of service rather than unsafe fallback and has no high-availability objective. |

## Maintenance

Update this document when a change adds a protected asset, trust seam, threat path, control-coverage
claim, or material residual risk. Change the owning architecture, requirement, defensive pattern, or
test policy when a mitigation itself changes; this register links that decision and does not approve
its implementation.

A missing required control fails closed and becomes issue work. Operational recovery guidance must
preserve append-only portfolio history and may not manufacture or infer a missing prior decision.
