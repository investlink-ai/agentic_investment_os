# Repository skills

Put project-specific skills in `.agents/skills/<skill-name>/SKILL.md`. Codex discovers this canonical
directory directly; `.claude/skills` points here so both coding agents read the same instructions.

Keep global or unrelated personal skills outside this repository. Add a repository skill only when a
repeatable workflow needs more detail than `AGENTS.md` should carry.
